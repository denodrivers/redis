# generated by octocov init
codeToTestRatio:
  code:
    - '**/*.ts'
    - '!tests/*.ts'
  test:
    - 'tests/*.ts'
coverage:
  paths:
    - coverage/lcov.info
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
repository: denodrivers/redis
